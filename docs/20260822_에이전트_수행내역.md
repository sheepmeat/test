# 20260822_에이전트_수행내역

## 1. 수행 개요

- 수행일: 2026-08-22
- 수행 항목: SafeNest mmWave V2 D1
- 범위: 24.17 GHz Six-Port 공개 레이더 취득·라이선스·스키마 감사, native I/Q 기반 위상/변위 어댑터, 동기 호흡 기준신호 추출
- 수행 결과: `PASS_WITH_LIMITATIONS`
- 핵심 판정: `YES_WITH_LIMITATIONS`
- 모델 학습: 수행하지 않음
- V2 feature 선택: 수행하지 않음
- D0, D2, CO₂, Thermal, Integration, D3, R1, Q1, I1: 수행하지 않음

## 2. Git 격리 및 정책 보호

- 작업 브랜치: `feature/mmwave-v2-d1-2417ghz-adapter`
- D1 시작 기준: M-PV0가 반영된 `origin/main`의 `fbfb22dbce2fe502fd59ad61332daac3a7a3bcd7`
- D1 커밋: `e9116ea3d3ccc4a6736925288ce4e7b4c44d0821`
- 이후 `origin/main`에 반영된 D0 커밋은 D1 브랜치의 조상이 아님
- 현재 변경 범위의 three-dot diff는 D1 어댑터·검증·증거·문서 파일만 포함
- M-PV0 frozen commit `18e4a4e86d6bf95795d6749a91ce303ad3f1c417`의 정책과 registry는 변경하지 않음
- 원자료 ZIP 및 추출된 MAT/CSV는 무시된 로컬 경로에만 보관하고 Git에 추가하지 않음

## 3. 권위 출처 및 취득 감사

권위 출처는 [Scientific Data 논문](https://www.nature.com/articles/s41597-020-0390-1), [Figshare canonical payload](https://figshare.com/articles/dataset/A_dataset_of_radar-recorded_heart_sounds_and_vital_signs_including_synchronised_reference_sensor_signals/9691544), [공개 MATLAB 코드](https://gitlab.com/kilinshi/scidata_vsmdb)를 사용했다.

| 항목 | 확인 결과 |
|---|---|
| Publication DOI | `10.1038/s41597-020-0390-1` |
| Collection DOI | `10.6084/m9.figshare.c.4633958.v1` |
| Canonical payload article DOI | `10.6084/m9.figshare.9691544.v1` |
| Payload | `datasets_scidata_vsmdb.zip` |
| Source URL | `https://ndownloader.figshare.com/files/17357702` |
| License | CC BY 4.0 |
| Byte size | `583572264` |
| MD5 | `801c13ae6daef54584ee4ba8fbabed19` |
| SHA-256 | `3869fb70a3dda0d810d97594399789e76d9c9e59627515c20170b83e3d915836` |
| Archive integrity | ZIP test passed |

MD5, byte 크기, SHA-256, archive integrity가 모두 확인되어 취득 차단 조건은 발생하지 않았다.

## 4. 실 payload 스키마 감사

- 총 11 subjects, 265 recordings
- archive member: MAT 265개, synchronized reference CSV 265개, metadata workbook 1개
- 모든 recording에서 `radar_I`, `radar_Q`, `respiration`, `Fs`, `measurement_info`가 존재
- radar I/Q/respiration 필수 배열 길이 불일치: 0건
- 필수 채널 non-finite 값: 0건
- 선택 ECG/PCG 채널은 전 recording에 존재하나 PCG 길이 불일치 16건은 warning으로 보존
- 실제 `Fs`: 2000 Hz 229건, 500 Hz 36건
- 샘플 수: 6,041–115,200
- 기록 길이: 약 12.08–57.60초
- reference CSV의 `m2a2;t2`는 공개 코드/문서 의미에 따라 ECG R-peak 및 T-wave-end sample index로 보존
- 논문·payload에서 호흡 기준신호의 물리 단위는 확정되지 않아 `UNVERIFIED_NATIVE_SENSOR_UNITS`로 기록
- per-sample timestamp는 원본 timestamp가 아니라 `time_s[i] = i / Fs` 규칙으로 재구성

Workbook의 source scenario는 generic class로 합치지 않고 다음 조건으로 보존했다.

`default`, `apnea`, `standing`, `after_sport`, `angle_variation`, `distance_variation`, `apnea_after_sport`, `artefact_speech`, `artefact_movement`, `lying`, `sitting`

`apnea` 표기는 source protocol의 breath-hold 조건을 보존한 것이며 임상적 apnea 또는 SafeNest label로 변환하지 않았다.

## 5. Native D1 어댑터

구현 파일: `adapters/mmwave_d1_2417ghz_adapter.py`

### 입력 및 채널 해석

- 기본 입력: payload의 명시적 `radar_I`, `radar_Q`
- Six-Port raw detector 입력이 명시적으로 제공되는 경우에만 `I = B5 - B6`, `Q = B3 - B4` 사용
- 배열 순서로 채널 의미를 추론하지 않음
- 필수 채널 누락, 부분 B3–B6, malformed 배열, non-finite 값, 길이 불일치, 지원하지 않는 `Fs`는 fail closed

### 변환

1. source가 요구하는 full-recording ellipse correction을 결정론적으로 적용
2. corrected trajectory에서 `atan2(Q, I)` 계산
3. `np.unwrap`으로 `native_unwrapped_phase_rad` 생성
4. `relative_displacement_m = phase * wavelength / (2π)` 계산
5. absolute distance가 아닌 relative displacement로 표시

Ellipse 보정은 공개 논문의 처리 요구사항을 반영했지만, 공개 구현이 P-code이므로 byte-identical 구현이라고 주장하지 않는다. 수치 안정화를 위한 conditioning은 ellipse fitting 내부에서만 사용하며, recording/window를 unit amplitude로 정규화하지 않는다.

### 기준신호 및 provenance

각 adapted record은 다음을 보존한다.

- subject
- session/recording 및 `measurement_info`
- source file
- condition 및 source quality rating
- source sampling rate와 sample range
- radar hardware/domain
- native radar signal 통계
- `respiration` reference channel, units, sample alignment
- adapter profile 및 quality/provenance flags

공통 8 Hz/240 samples 변환, local MAD normalization, D0/MR60 scaler, derivative, spectrum, autocorrelation, breathing score, RR target encoding, temporal hold logic는 적용하지 않았다.

## 6. 실행 및 검증 결과

- adapted recordings: 265 / 265
- synchronized respiration references: 265 / 265
- blocked recordings: 0
- required channel failures: 0
- optional PCG length warnings: 16
- runner 2회 실행 후 evidence checksum 동일
- D1 adapter/evidence 및 upstream mmWave 테스트: 29 tests passed
- M-PV0 standalone validator: passed
- Python compile check: passed
- `git diff --check`: passed

주요 증거 파일:

- `datasets/mmwave/manifests/M-PV0_D1_2417ghz_adapter/source_acquisition.json`
- `datasets/mmwave/manifests/M-PV0_D1_2417ghz_adapter/payload_inventory.json`
- `datasets/mmwave/manifests/M-PV0_D1_2417ghz_adapter/schema_audit.json`
- `datasets/mmwave/manifests/M-PV0_D1_2417ghz_adapter/adapter_contract.json`
- `datasets/mmwave/manifests/M-PV0_D1_2417ghz_adapter/recording_inventory.json`
- `datasets/mmwave/manifests/M-PV0_D1_2417ghz_adapter/exception_registry.json`
- `datasets/mmwave/manifests/M-PV0_D1_2417ghz_adapter/validation_result.json`
- `datasets/mmwave/manifests/M-PV0_D1_2417ghz_adapter/checksums.json`

## 7. 제한사항 및 후속 경계

1. payload가 500/2000 Hz로 혼재하므로 공통 sampling rate 결정은 R1/M-PV1에서 수행해야 한다.
2. radar 및 respiration native units는 authoritative metadata에서 확정되지 않았다.
3. 원본 per-sample timestamp가 없어 index/Fs 규칙으로 시간을 생성했다.
4. 공개 ellipse correction P-code와의 byte-level 동일성은 검증하지 않았다.
5. breath-hold metadata는 보존했지만 SafeNest APNEA proxy label은 생성하지 않았다.

D1의 기술적 산출물은 R1/M-PV1에서 cross-domain representation을 검토할 수 있는 상태다. 본 수행에서는 학습, 모델 선택, feature 선택, D2 의미론을 진행하지 않았다.

## 8. 이후 보고서 규칙

완료된 에이전트 작업은 앞으로 `docs/YYYYMMDD_에이전트_수행내역.md` 형식으로 기록한다. 각 보고서는 수행 범위, Git 격리, 취득·스키마 감사, 구현 결과, 검증 결과, 제한사항, 후속 경계를 포함한다.
