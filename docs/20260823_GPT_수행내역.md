# SafeNest mmWave V2 R3 수행내역 — 2026-08-23

## 1. 결과 요약

- 단계: **R3 only — breathing evidence + RR target + temporal-hold candidates**
- Gate: **`PASS_WITH_LIMITATIONS`**
- `R3_READY_FOR_M_PV1`: **YES**
- 모델 학습: 수행하지 않음
- 최종 representation/model/architecture/확률 threshold: 선택하지 않음
- 임상 apnea detector: 주장하지 않음

R3는 호흡 evidence, 연속 RR, temporal voluntary breath-hold proxy를 하나의 3-class target으로 합치지 않고 별도 semantics candidate로 동결했다. 최종 persistence duration과 model history는 M-PV1에 남겼다.

## 2. Git isolation 및 현재 main 정렬

- canonical repository: `https://github.com/sheepmeat/test.git`
- 현재 원격 main: `38ff2466280125bb7cdd073e163348fe4a9e9ec8` (`#120`, I1 skeleton)
- R2 exact head / PR #121: `37ea18d43780393b3f5c28deed655343ade59cd9`
- PR #121 상태: open, 아직 원격 `main`에 병합되지 않음
- R3 branch: `feature/mmwave-v2-r3-breathing-rr-temporal-hold`
- 로컬 통합 merge commit: `bfb43b7` — 현재 `origin/main`에 R2 exact head를 병합한 작업 기준
- 원격 PR merge/push: 수행하지 않음
- R3 PR base 권장: PR #121이 `main`에 반영된 뒤 `main`; 그 전에는 현재 로컬 통합 base의 출처를 PR 설명에 기록

R2 branch가 이전 R1 base에 있어 D0/Q2/I1의 현재 main 증거와 분리되어 있었으므로, R3 작업 기준은 `origin/main (#120) + R2 exact head (#121)`로 맞췄다. 충돌은 누적 수행내역 문서 1건뿐이었고 현재 main 쪽 Q1/Q2/D1/R1/I1 기록을 보존했다.

## 3. R2 handoff

- candidate set: `MMWAVE_V2_R2_REPRESENTATION_CANDIDATE_SET_V1`
- 상태: `BOUNDED_CANDIDATES_READY_FOR_M_PV1`
- F1: `MMWAVE_V2_R2_F1_NORMALIZED_SPECTRAL_V1`
- F2: `MMWAVE_V2_R2_F2_SPECTRAL_AUTOCORR_V1`
- F3: `MMWAVE_V2_R2_F3_TRACE_PLUS_QUALITY_DESCRIPTOR_V1`
- F1/F2/F3 winner: 선택하지 않음

## 4. 동결한 R3 identities

- `MMWAVE_V2_R3_TARGET_CONTRACT_SET_V1`
- `MMWAVE_V2_R3_BREATHING_EVIDENCE_TARGET_V1`
- `MMWAVE_V2_R3_RR_TARGET_V1`
- `MMWAVE_V2_R3_TEMPORAL_HOLD_TARGET_V1`
- `MMWAVE_V2_R3_MODEL_SUPERVISION_ELIGIBILITY_V1`

### Breathing evidence

D0의 authoritative A6 reference/RR audit와 voluntary non-breathing event timing을 사용했다. `BREATHING_REFERENCE_PRESENT`, `BREATHING_REFERENCE_ABSENT`, `BREATHING_REFERENCE_AMBIGUOUS`, `TARGET_UNAVAILABLE`을 명시적으로 분리했다. 레이더 amplitude, R2 feature score, RR category는 reference label이 아니다.

D0 TRAIN에는 162개 `PRESENT`, 156개 `AMBIGUOUS`가 있다. full-window hold가 실제 TRAIN compact window에 없으므로 `ABSENT`를 인위적으로 만들지 않았다. 이는 contract state가 없는 것이 아니라 관측된 TRAIN evidence의 한계다.

### RR

`rr_bpm`을 primary continuous target으로 유지했다. D0에서는 기존 A4 `MOVESENSE_CHEST_ACC` spectral-peak reference method와 0.1–0.7 Hz band를 상속했다.

- D0 RR target available: 162
- D0 RR target unavailable: 156 (hold/transition overlap)
- unavailable RR: `rr_bpm: null` + explicit reason
- `0 bpm` sentinel: 사용하지 않음
- MR60 `breath_rate_raw`: 사용하지 않음

### Temporal hold

event-relative fields는 onset/offset distance, hold elapsed time, baseline, recovery, transition state를 보존한다. D0 TRAIN의 156 event-overlap rows는 전체 window `APNEA`로 덮지 않고 다음 상태로 기록했다.

- `HOLD_ONSET_AND_RECOVERY_WITHIN_WINDOW`: 8
- `HOLD_ONSET_WITHOUT_RECOVERY_IN_WINDOW`: 123
- `HOLD_ACTIVE_AT_WINDOW_START_AND_RECOVERY`: 25
- `NO_HOLD_EVENT_IN_WINDOW`: 162

previous valid breathing baseline이 없는 시작 상태는 hold candidate로 승격하지 않는다. recovery가 관측되면 event를 종료하고 다음 recording/window로 상태를 넘겨 carry-over하지 않는다. 최종 persistence threshold는 `DEFERRED_TO_M_PV1`이다.

## 5. 데이터 governance

### D0

- frozen split: `MMWAVE_V2_D0_SUBJECT_SPLIT_V1`
- 사용 범위: TRAIN only
- subjects: 66
- recordings: 264
- windows: 318
- VAL 사용: 아니오
- `D0_SUBJECT_HELDOUT` 사용: 아니오
- M-N6 excluded 16 subjects 사용: 아니오
- D2 사용: 아니오
- MR60 supervised use: 아니오

### D1

- recordings: 265 / subjects: 11
- synchronized `respiration` channel declared with compact adapter statistics: 265
- exact source condition `apnea`: 25
- breath-hold protocol rows including `apnea_after_sport`: 35
- defensible onset/offset boundaries in compact evidence: 0
- breathing/RR/temporal target generated: 0

D1 raw synchronized respiration waveform은 현재 compact evidence에 materialize되어 있지 않다. 따라서 D1 reference channel 존재는 audit했지만 periodicity/RR/temporal boundary를 추측하지 않고 `TARGET_UNAVAILABLE`로 남겼다. D1 `apnea` 문자열은 SafeNest APNEA-proxy로 자동 매핑하지 않았다.

## 6. Quality 및 supervision eligibility

Q2 `MMWAVE_V2_Q2_INPUT_AVAILABILITY_CONTRACT_V1`의 precedence를 그대로 참조했다.

```text
presence → input availability / quality → physiology
```

정확한 flat/non-finite/timing-invalid/gap 입력은 physiology target 실행 및 model supervision을 허용하지 않는다. 반대로 A6 `AMBIGUOUS`는 입력 품질 실패가 아니라 annotation transition state이므로 Q2 unavailable로 오인하지 않고 transition analysis에 보존했다.

## 7. Validation

- R3 semantic safety tests: **9 passed**
- R3 runner: **PASS_WITH_LIMITATIONS**
- R3 standalone validator: **PASS_WITH_LIMITATIONS**, errors 0
- D0 target rows validated: **318**
- D1 target rows validated: **265**
- deterministic rerun test: passed
- raw/derived waveform committed: 아니오

주요 명령:

```text
python3 -m unittest discover -s tests -p 'test_mmwave_r3_target_contracts.py'
python3 scripts/run_mmwave_r3_target_contracts.py
python3 scripts/validate_mmwave_r3_target_contracts.py
```

## 8. M-PV1 경계 및 제한사항

M-PV1이 결정할 항목:

- F1/F2/F3 중 common representation/profile
- exact input/window/history contract
- breathing/RR target binding과 D1 waveform materialization
- deterministic vs learned temporal-hold composition
- bounded model families
- calibration 및 threshold strategy

R3에서 하지 않은 것:

- direct `NORMAL / RAPID / APNEA` primary target
- clinical/sleep apnea claim
- D2 acquisition inspection 또는 inference
- MR60 supervised target creation
- Q3 false-positive evaluation
- I2/I3 replay/regression
- model training/architecture selection
- probability 또는 persistence threshold tuning

현재 판정은 D1 target waveform materialization과 최종 persistence/history가 남아 있으므로 `PASS_WITH_LIMITATIONS`다. 그 제한을 명시한 상태에서 R3는 M-PV1에 전달할 수 있다.

---

## I3 — presence / gap / freeze / stale fail-closed regression

- 단계: **I3 only — runtime semantic fail-closed regression**
- Gate: **`PASS_WITH_LIMITATIONS`**
- `I3_INTEGRATION_LANE_COMPLETE`: **YES**
- 모델 학습 / V1·V2 inference / Q3 / M-PV1 / D2: 수행하지 않음
- Q2 임계값 fork: 없음

### Scope

I3는 동결된 I1 런타임 의미 경계, I2 JSONL replay harness, Q2 `evaluate_availability`를 재사용해 presence → availability → physiology 선행이 실행 가능한지 검증한다. 생리 결과를 만들어 배관을 테스트하지 않는다.

### Git isolation

`origin/main` `e84d802e5b9aa28e6729a02b304f1f70043f89c3`에서 `feature/mmwave-v2-i3-fail-closed-regression`를 분기했다. R2/R3/D2 미병합 ancestry 없음.

### Identities

- `MMWAVE_V2_I3_FAIL_CLOSED_REGRESSION_CONTRACT_V1`
- `MMWAVE_V2_I3_REGRESSION_MATRIX_V1`
- `MMWAVE_V2_I3_RUNTIME_SAFETY_GATE_V1`

### Presence

false / unknown production → `PRESENCE_SUPPRESSED`. true+valid → `PHYSIOLOGY_ELIGIBLE` (physiology_executed=false). true+invalid → `INPUT_UNAVAILABLE`. false+invalid도 presence-first.

### Synthetic Q2

CLEAN_VALID는 eligible. LARGE_GAP / SOURCE_FREEZE / STALE_SOURCE / FLAT_EXACT / JITTER_PLUS_LARGE_GAP / REPUBLICATION_TO_FREEZE는 `INPUT_UNAVAILABLE`. typical jitter와 단독 republication, 저진폭 동적 신호는 그 이유만으로 거부되지 않는다. RECOVERY_WARMUP 샘플이 있는 window는 unavailable. 단일 정상 패킷이 freeze window를 eligible로 되돌리지 않는다.

### Historical

I2 7개 대표 세션. 1719 replayed / 1 reject. 95-run과 3598-run prefix quality-only window: `INPUT_UNAVAILABLE` + `SOURCE_FREEZE`. timestamp collision은 I2 typed reject + Q2 `TIMESTAMP_UNRESOLVED`. phase-age 부재는 `SOURCE_STALE`. 생리 정답으로 해석하지 않음.

### Fallback / session

INVALID→NORMAL/APNEA 불가. no-person→APNEA/RR0 불가. class confidence로 unavailable 덮어쓰기 불가. 세션 reset 누수 없음. seq gap 보간 없음. compact checksum 결정론.

### Limitations

Pi receive clock 부재, 3598-run 64-row prefix, 일부 firmware null, eligible 이벤트는 mock/not evaluated.

### Validation

`tests/test_mmwave_i3_fail_closed_regression.py` 28 passed. validator `PASS_WITH_LIMITATIONS`.

---

# M-PV1 수행내역 — Public Multidomain Common Data / Representation / Target / Abstention Contract Freeze

## 결과 요약

- 기준: 최초 assignment `origin/main` `e84d802e5b9aa28e6729a02b304f1f70043f89c3` (I2 `#122`)
- rebase: 최신 `origin/main` `b77e32a` (`#126` I3). I3 수행내역은 누적 보존. I3를 M-PV1 학습/선택 입력으로 쓰지 않음.
- 브랜치: `feature/mmwave-v2-m-pv1-common-contract`
- Gate: **`PASS_WITH_LIMITATIONS`**
- `M_PV1_READY_FOR_M_PV2`: **YES**
- 학습/모델 선택/seed sweep/threshold tuning/calibration/INT8/TFLite: 수행하지 않음
- D2 payload 의미 접근·feature·target·inference·selection: 수행하지 않음
- MR60 supervised physiology: 사용하지 않음

## 선행 조건과 데이터 역할

승인된 M-PV0, D0, D1, R1, R2, R3, Q2, I1 evidence를 상속했다. I2는 직접 prerequisite로 사용하지 않았고 I3 조상은 사용하지 않았다. D3 adapter는 없어 `NOT_INCLUDED_NON_BLOCKING`이다.

`MMWAVE_V2_D0_SUBJECT_SPLIT_V1`은 변경하지 않았고 D0 TRAIN 66 subjects / 318 base contexts만 사용했다. M-N6 excluded 16 subjects, D0 VAL, D0 internal heldout은 M-PV1 selection에 사용하지 않았다. D1은 subject-level `MMWAVE_V2_M_PV1_D1_DEV_SUBJECT_SPLIT_V1`로 seed `20260823`, 8/3 분할을 고정했다.

## D0 target granularity resolution

R3의 D0 TRAIN whole-window coverage는 `PRESENT=162`, `AMBIGUOUS=156`, `ABSENT=0`이었다. whole-window PRESENT/ABSENT classifier를 만들지 않고, A6 authoritative voluntary hold interval과 30초 context 교집합 안의 완전한 5초 event-relative interval만 추가했다.

- event-relative `BREATHING_REFERENCE_ABSENT`: 133
- 5초 미만 교집합 retained `AMBIGUOUS`: 23
- whole-window ABSENT: 0 (명시적으로 노출)
- 레이더 amplitude/model output으로 target timing을 만들지 않음

## D1 reference materialization

canonical Figshare payload `10.6084/m9.figshare.9691544.v1`, file ID `17357702`를 재취득하고 frozen size/MD5/SHA-256을 확인했다. canonical D1 adapter로 265/265 recording을 읽고 synchronized `respiration` reference를 10 Hz anti-aliased compact analysis에 사용했다. 30초 model-ready context는 244개, 21개는 짧아 audit-only다. D1 `PRESENT/RR available=236`, `AMBIGUOUS=8`, temporal hold는 `UNAVAILABLE`이다. `apnea` source string은 target으로 자동 매핑하지 않았다. raw archive/waveform은 ignored 경로에만 있고 commit하지 않는다.

## Representation / input / temporal contract

- common rate: **10 Hz**
- model context: **30 s / 300 samples**
- evaluation stride: **5 s**
- scalar: F2 spectral+autocorrelation 25-feature fixed order + quality sidecar
- trace: F3/R1 `[B,300,1]` + valid mask + native scale descriptors
- hybrid: A/B의 결정적 조합만 조건부 허용
- F1: ablation baseline only; original scale retained; window-local MAD divide-only/source gain matching 금지
- short D1: fake padding 없이 audit-only
- history: previous valid breathing baseline을 same subject/recording/session 안에서만 sequential composer state로 유지
- final persistence threshold: later development-only calibration으로 유보

## Target / quality / balancing

공통 target은 `PRESENT/ABSENT/AMBIGUOUS/TARGET_UNAVAILABLE` breathing evidence, continuous `rr_bpm` with explicit unavailable reason, event-relative voluntary breath-hold proxy다. Q2 hard invalid gate는 physiology보다 먼저 실행되며, synthetic Q1/Q2 corruption은 `INPUT_UNAVAILABLE/ABSTAIN`만 학습한다. source weight는 D0 `0.75`, D1 `0.25`, source 내부 subject-aware inverse-eligible weighting, task별 explicit mask, clean 대비 synthetic 최대 10%로 동결했다.

## 검증 및 산출물

생성기를 두 임시 출력에 반복 실행해 deterministic byte/checksum 일치를 확인했다. focused validator 결과는 `PASS_WITH_LIMITATIONS`, errors 0, example 716개, D0 event-relative absent 133개, D1 model-ready contexts 244개다. 기계 판독 산출물은 `config/mmwave/m_pv1_public_multidomain_contract.json` 및 `datasets/mmwave/manifests/M-PV1_public_multidomain_contract/`에 있다. 보고서는 `docs/mmwave/20260823_SafeNest_mmWave_M-PV1_Public_Multidomain_Common_Contract_Report_KO_01.md`다.

---

# M-PV1 CORRECTIVE 수행내역 — Target/Context Alignment

최초 M-PV1 independent review에서 30초 global input에 arbitrary internal 5초 event target을 붙인 semantic mismatch가 확인되어 기존 판정을 보류했다. 기존 133개 event-relative ABSENT를 수량 유지 목적으로 보존하지 않고, `[t-30,t]` causal context의 마지막 `[t-5,t]`만 공통 breathing target으로 동결했다.

- D0 unique inputs: 318
- D0 PRESENT/ABSENT/AMBIGUOUS: 162 / 116 / 40
- 기존 후보 중 corrective audit-only: 17
- D1 unique model-ready inputs: 244
- D1 PRESENT/ABSENT/AMBIGUOUS: 236 / 0 / 8
- D1 short audit-only: 21
- total unique model inputs: 562
- target records: 2,248; duplicate input overlays: 0
- clean quality denominator: 562 unique model inputs

F2 global scalar는 final 5초 위치를 보존하지 않으므로 breathing task를 지원하지 않게 했고, trace/TCN 및 hybrid만 breathing task를 지원한다. temporal hold는 direct neural supervision이 아닌 deterministic post-breathing composer로 동결했고, RR은 별도 30초 reference interval로 분리했다. 모든 model-ready row에 `model_input_id`, `target_id`, `target_task`, target interval, fixed anchor, causality, tensor contract, task mask, provenance를 기록했다.

추가 corrective test 9개와 focused validator를 실행했다. deterministic generation, fixed anchor, identical PRESENT/ABSENT semantics, causal/no-future, D1 alignment, no-padding audit-only, unique quality accounting, task compatibility, D2/MR60/model-training safety가 모두 통과했다. 최종 corrective gate는 `PASS_WITH_LIMITATIONS`, `M_PV1_READY_FOR_M_PV2=YES`다. M-PV2 학습·선정·튜닝·양자화는 시작하지 않았다.

---

# M-PV2 Bounded Candidate Training 수행내역 — 2026-08-23

## 수행 목적

M-PV1 corrective 계약을 전제로 공개 D0/D1 데이터에서 M-PV2 후보군만 학습하고, 재현성·누수·fail-closed·성능 증거를 남겼다. 최종 모델 선택, D2 사용, MR60 지도학습, 양자화, Pi 배포는 수행하지 않았다.

## 저장소 및 사전 확인

- 대상 저장소: `https://github.com/sheepmeat/test.git`
- 기준 브랜치/커밋: `origin/main` / `71475aa37b37ae5e643461d804f2ecdcbaea34cd`
- 작업 브랜치: `feature/mmwave-v2-m-pv2-candidate-training`
- M-PV1 schema: `M-PV1.2_CORRECTIVE_ALIGNMENT`, gate `PASS_WITH_LIMITATIONS`, `m_pv1_ready_for_m_pv2=true`
- 고정 membership: D0 318, D1 244, 총 562 unique model input, duplicate overlay 0
- D1 split: 8명 `D1_DEV_TRAIN` / 3명 `D1_DEV_VAL`, subject leakage 없음
- D2 semantic access/feature extraction/target use/inference/selection: 모두 잠금
- D0 VAL은 role contract 허용 목록에는 있으나 frozen M-PV1 model-ready rows가 TRAIN-only이므로 membership를 바꾸지 않기 위해 M-PV2에서 사용하지 않음

## 구현 및 실행

- `config/mmwave/m_pv2_candidate_training_contract.json`을 training 전에 동결했다.
- accepted R1 trace와 D1 adapter를 메모리에서 재생성하고 고정 30초/300샘플 context와 R2 F2/F3 descriptors를 만들었다.
- tensor lineage, membership, scaler fit scope, target masks를 evidence에 기록했다. raw waveform/tensor cache는 저장소에 넣지 않았다.
- Family A(F2 MLP RR/quality), Family B(trace TCN breathing/RR/quality), Family C(hybrid)를 seed 11/23/47로 각각 학습했다.
- Q2 synthetic quality corruption은 training clean inputs의 10% 이하로 고정 생성했고 physiology target은 수정하지 않았다.
- Family B seed 11을 새 프로세스에서 재학습해 canonical parameter SHA 일치를 확인했다.

## 검증 결과

- M-PV2 validator: `PASS_WITH_LIMITATIONS`
- primary candidate runs: 9/9
- final selection / selected float model: `NO` / `NO`
- D2 semantic use/inference: `NO` / `0`
- MR60 supervised use: `NO`
- Q2 invalid false acceptance: 기록된 validation corruption에서 0.000
- event F1: M-PV3 이후로 연기

실행한 검증 명령과 실제 결과는 `docs/mmwave/20260823_SafeNest_mmWave_M-PV2_Candidate_Training_01.md`와 `datasets/mmwave/manifests/M-PV2_candidate_training/validation_result_validator.json`에 남겼다.

## 생성된 핵심 경로

- `datasets/mmwave/manifests/M-PV2_candidate_training/`
- `models/mmwave/m_pv2/family_a/`, `family_b/`, `family_c/`
- `scripts/mmwave_m_pv2_candidate_training.py`
- `scripts/validate_mmwave_m_pv2_candidate_training.py`
- `tests/test_mmwave_m_pv2_candidate_training.py`

## 결론

M-PV2의 역할인 “후보군을 bounded하게 만들고 M-PV3가 선택할 수 있는 증거를 준비하는 것”은 완료했다. 어느 family/seed도 최종 모델로 승격하지 않으며, 다음 단계는 별도 M-PV3 selection contract를 동결한 뒤 진행해야 한다.
